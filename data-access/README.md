# Database Access POC

1. Create database
```shell
$ docker-compose up
```

2. Create schema, run the scripts from create-tables.sql

3. Set env vars
```shell
$ export DBUSER=admin
$ export DBPASS=admin
```